namespace DesignPatternsTutorial.src.DesignPatterns.Creational.Builder.Components
{
    public class Dashboard
    {
        public bool _hasRevCounter;

        public Dashboard(bool hasRevCounter)
        {
            _hasRevCounter = hasRevCounter;
        }
    }
}